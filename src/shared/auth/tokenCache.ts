// Implementación oficial de Clerk sobre expo-secure-store (AFTER_FIRST_UNLOCK,
// recuperación de items corruptos) — no reimplementar lo que el SDK ya cubre.
export { tokenCache } from '@clerk/clerk-expo/token-cache';
